为桌面排行榜前三名加入独立的行间分隔线，让榜首组合自成一区，同时不破坏表格列宽与对齐。

1. 修改 `src/components/DesktopRankingTable.tsx`
   - 在第 1 名行之后、第 2 名行之后分别插入一条全宽的分隔行。
   - 分隔行使用单个 `<TableCell colSpan={5}>`，并设置 `p-0 border-0`，避免影响已有的 `table-fixed` 列宽和表头对齐。
   - 分隔条为短渐变条：
     - 第 1–2 名之间：使用 `accent` token（金黄色）强调冠军优势。
     - 第 2–3 名之间：使用 `primary` token（亚军/季军色）过渡。
   - 高度跟随密度切换：`compact` 约 `h-1.5`，`comfortable` 约 `h-2.5`。
   - 保持现有前三名行的背景、左侧竖条、头像、徽章等样式不变；第 4 名以下的行样式也不变。

2. 兼容性检查
   - 当排行榜不足 3 人时，只显示实际存在的分隔行。
   - 分隔行本身不参与 hover、斑马纹和统计对齐。

仅变更 `DesktopRankingTable` 组件，不做后端或翻译改动。